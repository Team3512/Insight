[access]
	inheritFrom = All-Users
[submit]
	action = rebase if necessary
	mergeContent = false
[project]
	description = Performs image processing on an MJPEG stream, then forwards it to the DriverStationDisplay and sends control commands to the robot.
